Look at the vertical separation between nodes e8580effc15fae65 and d42b708651cac365. This is the minimal vertical separation I ever want to have between nodes. Some nodes that are too close (e.g. the recently placed da365617ca4568c4 and f0a29bcff40c5ca9 nodes) are too close and it cuts off the status text that Node-RED shows below the top node. Update all layout guidance accordingly.
Additionally, Node-RED generally snaps nodes to a grid system of size 20. So also update all layout guidance so that it respects this. If it's helpful, some examples of grid-aligned nodes are all the nodes in group 3c5a78c3963230f2 (incl the group itself) and some not-grid-aligned nodes are 253d46e1a3d43188 4580f1613e8529e8 and 6bbdfe6c1af815b0. Also the positions you write to json should only be integers, don't include decimal components, even if it's `.0`.
